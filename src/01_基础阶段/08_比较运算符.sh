#!/bin/bash

# 数值比较
[ $a -eq $b ]  # 等于
[ $a -ne $b ]  # 不等于
[ $a -gt $b ]  # 大于
[ $a -lt $b ]  # 小于
[ $a -ge $b ]  # 大于等于
[ $a -le $b ]  # 小于等于

# 字符串比较
[ "$str1" = "$str2" ]   # 相等
[ "$str1" != "$str2" ]  # 不相等
[ -z "$str1" ]          # 判断是否为空字符串
[ -n "$str1" ]          # 判断是否非空字符串
